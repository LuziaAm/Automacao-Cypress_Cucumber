
export default{

    usuario: function(){

        const username = 'standard_user'
        const password = 'secret_sauce'

        let user = {
            username: `${username}`,
            password: `${password}`
        }

        return user
    }
}